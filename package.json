{
  "name": "socket-browser",
  "version": "0.1.0",
  "description": "Next-generation browser that generates UI from Socket Agent APIs using LLMs",
  "main": "src/main.js",
  "scripts": {
    "start": "electron src/main.js --no-sandbox",
    "dev": "electron src/main.js --dev --no-sandbox",
    "build": "electron-builder",
    "test": "jest"
  },
  "keywords": ["electron", "browser", "socket-agent", "llm", "ui-generation"],
  "author": "Socket Agent Team",
  "license": "MIT",
  "devDependencies": {
    "electron": "^26.0.0",
    "electron-builder": "^24.0.0",
    "jest": "^29.0.0"
  },
  "dependencies": {
    "openai": "^4.20.0",
    "axios": "^1.5.0"
  }
}